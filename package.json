{
	"name": "beatmods-cn",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "python inline_database/upgrade.py",
		"deploy": "python inline_database/upgrade.py && wrangler deploy",
		"dev": "python inline_database/upgrade.py && wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.59.2"
	}
}